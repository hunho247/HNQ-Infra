apiVersion: hnq.dev/v1
kind: ServiceRelease
metadata:
  name: hocmon-clinic
spec:
  category: app
  chart: webservice
  # Nạp thêm /registry/apps/hocmon-clinic/config/config-<env>.yaml
  config: true
  environments:
    # prod bật ở P6, sau khi P5 xong (PLAN §16, cửa chặn 2).
    - env: dev
  requiredSecrets:
    - name: hocmon-clinic-backend
      keys: [DB_USER, DB_PASS, JWT_SECRET]
    - name: hocmon-clinic-keystore
      keys: [private.pem, public.pem]
    - name: hocmon-clinic-registry
      keys: [.dockerconfigjson]

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: server-control-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://gitlab.com/hnq-tech/hnq-infra.git
    targetRevision: main
    path: infra/helm/admin/server-control
    helm:
      releaseName: server-control
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: server-control-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
